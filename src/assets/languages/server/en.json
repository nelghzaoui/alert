{
  "server": {
    "list": {
      "title": "Choose your server",
      "actions": {
        "redirect": "Login to {{ server }}",
        "update": "Update {{ server }}",
        "remove": "Remove {{ server }}"
      }
    }
  }
}
